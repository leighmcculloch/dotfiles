After cloning this project, you can run the following to link these dotfiles
into your home directory:

    rake

Be warned: this will overwrite any existing .tmux.conf and .tmux/ files you
have in your home directory.

Uses `tpm` (tmux plugin manager) to manage plugins.

